template: amazon_ebs

var_files:
  - default_linux
  - amazon_ebs_bionic

# override_vars:
#   instance_type: m5.metal

script_directories:
  - ubuntu-bionic
  - worker-runner-linux
  - docker-worker-linux

platform: linux
