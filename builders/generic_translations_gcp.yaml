template: googlecompute
platform: linux

builder_var_files:
  - taskcluster_version_latest  # new file as non-latest still needed for d-w
  - default_linux
  - translations_gcp
  # - default_community  # needed? seems specific to d-w
  - googlecompute_cuda

script_directories:
  # - ubuntu-bionic  # needed with provided image? maybe just packages and a few others?
  - worker-runner-linux
  - generic-worker-linux
