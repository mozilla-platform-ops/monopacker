template: googlecompute
platform: linux

builder_var_files:
  - taskcluster_version_latest  # new file as non-latest still needed for d-w
  - default_linux
  - translations_gcp
  - googlecompute_cuda

script_directories:
  - ubuntu-bionic-minimal  # see if we can get away with fewer things, fall back to non minimal
  - worker-runner-linux
  - generic-worker-linux
  - worker-runner-gw-systemd  # TODO: not complete yet
